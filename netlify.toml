[build]
  command = "npm run build"
  publish = "out"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

# Password protection - Configure this in Netlify UI:
# Site Settings > Visitor Access > Password Protection
